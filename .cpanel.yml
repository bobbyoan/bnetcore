---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/bnetcore/public_html/
    - /bin/rsync -av --delete --exclude='.git' --exclude='.github' --exclude='.cpanel.yml' --exclude='node_modules' ./ $DEPLOYPATH
